name: Docker

on:
  push:
    # See release.yml for why this regex-style glob is correct in
    # GitHub Actions tag filters. Same pattern, same justification.
    tags:
      - "v[0-9]+.*"

permissions:
  contents: read
  packages: write
  # id-token: write is required for cosign keyless signing of the
  # image digest via Sigstore + GitHub OIDC.
  id-token: write

concurrency:
  group: docker-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      # setup-qemu enables cross-platform image builds via emulation.
      # Required to produce linux/arm64 manifests on a linux/amd64 runner.
      - uses: docker/setup-qemu-action@v3

      # setup-buildx switches the build to the docker-container driver,
      # which is required for SBOM and SLSA provenance attestations.
      # The default docker driver does not support attestations and
      # fails the build with "Attestation is not supported for the
      # docker driver".
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3.7.0

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/garagon/mcp-aguara
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        id: build
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Multi-arch image: native amd64 + arm64 (Apple Silicon,
          # Linux ARM, AWS Graviton, GitHub ARM runners) without
          # forcing --platform.
          platforms: linux/amd64,linux/arm64
          # Inject the release version into the binary so
          # `aguara-mcp --version` inside the container reports the
          # actual published version instead of the Dockerfile default
          # (dev). steps.meta.outputs.version is the semver-stripped
          # form (0.6.0), matching what GoReleaser injects into the
          # tar.gz binaries via {{.Version}}.
          build-args: |
            VERSION=${{ steps.meta.outputs.version }}
          # SBOM (SPDX) and SLSA build provenance attestations attached
          # to the OCI image index. Read with
          # `docker buildx imagetools inspect`.
          sbom: true
          provenance: mode=max

      # Sign the image digest produced by build-push-action. Signing
      # the digest (not the tag) ensures verification cannot be tricked
      # by a tag being later repointed.
      - name: Sign image
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          set -euo pipefail
          for tag in $TAGS; do
            cosign sign --yes "${tag}@${DIGEST}"
          done
